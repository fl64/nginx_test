version: '3.4'

services:
  nginx_test:
    container_name: nginx_test
    build: .
    #entrypoint: ["/bin/sh","-c","sleep infinity"]
    environment:
    - TEST_NGINX_PORT=8080
    - TEST_NGINX_MSG=test message!
    - TEST_NGINX_BC=lightblue
    restart: always
    ports:
    - 8080:8080
